version: "3.9"
services:
  streaks-cli:
    build: .
    container_name: "streaks-cli"
    restart: always
    environment:
      - TZ=Europe/Paris
    volumes:
      - ./config.yml:/cli/config.yml
      - ./.env:/cli/.env
